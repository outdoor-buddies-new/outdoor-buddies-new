/**
 * Page that displays the AddProfileForm for Users to create a new Profile
 * Users can only create One Profile
 */

'use client';

import AddProfileForm from '../../../components/AddProfileForm';

export default function AddProfilePageRoute() {
  return (
    <main>
        <AddProfileForm />
    </main>
  );
}
